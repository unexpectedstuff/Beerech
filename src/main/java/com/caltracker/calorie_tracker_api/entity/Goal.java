package com.caltracker.calorie_tracker_api.entity;
//I dunno why i'm doing this to myself, but enum is better than string in that case. 
public enum Goal {
    LOSE_WEIGHT,
    GAIN_MUSCLE,
    MAINTAIN
}
